---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
  <article class="inventory-content">
    <div class="item-header">
      <div class="item-header-info">
        <h1>{frontmatter.title}</h1>
        <p class="item-desc"><em>{frontmatter.description}</em></p>
        <p class="item-meta">
          📅 Date Added: {frontmatter.postDate.toLocaleDateString("en-US", {
            month: "long",
            day: "numeric",
            year: "numeric",
          })}
        </p>
      </div>

      {frontmatter.image && frontmatter.image.url && (
        <div class="item-featured-image">
          <img src={frontmatter.image.url} alt={frontmatter.image.alt} loading="eager" />
        </div>
      )}
    </div>

    <div class="markdown-body">
      <slot />
    </div>
  </article>
</BaseLayout>

<style>
  .item-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
    margin-bottom: 1rem;
  }

  .item-header-info {
    flex: 1;
    min-width: 0;
  }

  .item-header-info h1 {
    margin: 0 0 0.4rem;
    border-bottom: none;
    padding-bottom: 0;
  }

  .item-desc {
    color: var(--text-muted);
    font-size: 1.15rem;
    line-height: 1.5;
    margin: 0 0 0.5rem;
  }

  .item-meta {
    color: var(--text-muted);
    font-size: 0.9rem;
    margin: 0;
    opacity: 0.85;
  }

  .item-featured-image {
    width: 200px;
    height: 180px;
    flex-shrink: 0;
    border: 1px solid var(--border-card);
    background-color: var(--bg-card);
    padding: 8px;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .item-featured-image img {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
    border-radius: 2px;
    display: block;
  }

  @media screen and (max-width: 635px) {
    .item-header {
      flex-direction: column-reverse;
      align-items: stretch;
      gap: 1.25rem;
    }

    .item-featured-image {
      width: 100%;
      height: 180px;
    }
  }

  .markdown-body hr {
    margin: 1.5rem 0 2rem;
  }
</style>